# Facemaskdetection
Face Mask Detection system using Deep Learning and Computer Vision to classify whether a person is wearing a mask or not. A CNN model is trained on images labeled with_mask and without_mask. Built with Python, TensorFlow, OpenCV, and NumPy, the system preprocesses images and predicts mask usage for automated safety monitoring.
